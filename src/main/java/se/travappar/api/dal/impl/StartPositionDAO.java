package se.travappar.api.dal.impl;

import se.travappar.api.dal.AbstractDAO;
import se.travappar.api.model.StartPosition;

public class StartPositionDAO extends AbstractDAO<StartPosition> {
    public StartPositionDAO() {
        super(StartPosition.class);
    }
}
